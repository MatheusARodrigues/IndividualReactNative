import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  button: {
    backgroundColor: '#fff',
    opacity: 0.8,
    width: '90%',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
    height: 50,
  },

  textButton: {
    fontSize: 35,
    color: "#fb9b04",
    fontWeight: '900'
  }
});